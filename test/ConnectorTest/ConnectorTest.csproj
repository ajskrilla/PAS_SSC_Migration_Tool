<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <!-- Reuse the real connector source so we test the SAME code the app runs. -->
  <ItemGroup>
    <Compile Include="../../src/Connectors/PasConnector.cs" />
    <Compile Include="../../src/Connectors/TenantCredentials.cs" />
  </ItemGroup>
</Project>
